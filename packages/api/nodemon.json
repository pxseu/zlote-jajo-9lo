
{
	"watch": ["src", ".env", "package.json", "tsconfig.json"],
	"ignore": ["dist/*"],
	"ext": "ts,ejs",
	"exec": "yarn clean && yarn build && node -r dotenv/config . || exit 1",
	"delay": 500,
	"signal": "SIGTERM"
}